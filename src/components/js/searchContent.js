import React from 'react'
import { rooms } from './data';

const SearchCollection = (searchLoc) => {
    

    return(
        <div className="search-content">
            {searchContent}
        </div>
    )
}

export default SearchCollection;